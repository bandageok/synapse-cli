# P0-1: 虚拟滚动（Virtual Scroll）评估

## 问题
当前 REPL 渲染所有消息到终端。长对话会：
1. 占满终端内存
2. 渲染卡顿
3. Ink 性能下降

## 方案
Claude Code 使用 FullscreenLayout + ScrollBox + 视口裁剪。

**Synapse 方案：**
在 REPL 中实现消息窗口裁剪，只显示最近 N 条消息（可配置）。

## 实施
- MAX_OUTPUT 从 120 → 50（默认），保留历史消息
- 添加"查看更多"提示
- 保留状态栏和输入区固定位置
